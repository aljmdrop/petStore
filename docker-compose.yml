version: '3.8' 
services: 
  frontend: 
    image: aljmdrop/petstore-frontend:latest 
    ports: 
      - "80:3000" 
    restart: always
  backend: 
    image: aljmdrop/petstore-backend:latest 
    ports: 
      - "8080:3001" 
    restart: always
  
  db:
    image: aljmdrop/petstore-db:latest
    restart: always
    ports:
      - "3307:3306"
      